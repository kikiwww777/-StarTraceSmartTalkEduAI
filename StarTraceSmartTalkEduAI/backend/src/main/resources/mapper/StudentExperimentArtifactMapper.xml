<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eduai.Mapper.StudentExperimentArtifactMapper">

    <resultMap id="BaseResultMap" type="com.eduai.pojo.entity.StudentExperimentArtifact">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="attempt_id" property="attemptId" jdbcType="BIGINT"/>
        <result column="artifact_type" property="artifactType" jdbcType="VARCHAR"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="url" property="url" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="LONGVARCHAR"/>
        <result column="content_type" property="contentType" jdbcType="VARCHAR"/>
        <result column="file_size" property="fileSize" jdbcType="BIGINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, attempt_id, artifact_type, title, url,
        content, content_type, file_size, created_at
    </sql>

    <insert id="insert" parameterType="com.eduai.pojo.entity.StudentExperimentArtifact"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO student_experiment_artifacts (
            attempt_id, artifact_type, title, url,
            content, content_type, file_size, created_at
        ) VALUES (
            #{attemptId}, #{artifactType}, #{title}, #{url},
            #{content}, #{contentType}, #{fileSize}, #{createdAt}
        )
    </insert>

    <select id="selectByAttemptId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM student_experiment_artifacts
        WHERE attempt_id = #{attemptId}
        ORDER BY id ASC
    </select>

</mapper>









































